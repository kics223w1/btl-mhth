Task 2 – Explicit reachability

Goal
- Enumerate all reachable markings via BFS from the initial marking.

Run
```bash
python -m app.cli explicit --pnml pnml/simple_fork_join.pnml
```

Output
- JSON with total state count and runtime in seconds.


